# 第04回　Python統計処理入門（Colab / Pandas / NumPy）

> 計算は機械に、判断は人間に ―― 今日から、あなたがコードを書く

---

## この回のゴール

- データを「表（DataFrame）」として読み込み、列を取り出せる
- 第2・3回で手計算した代表値・ばらつきを、**コードで再現**できる
- `describe()` / `groupby()` / `isna()` / ヒストグラムで、基礎集計を自分で実行できる

到達目標との対応：**G3（Pythonで自律的に解析）の本丸**。第5回以降の相関・推定・検定は、すべてこの上に乗る。

---

## 0. 今日から方式が変わる

第1〜3回は「読んで▶を押すだけ」だった。今日からは、**空欄 `____` を自分で埋める**。とはいえコードの大半は用意してあるので、入れるのは列名やメソッド名だけ。

!!! tip "エラーは失敗ではない"
    エラーメッセージは「ここが違うよ」という**お知らせ**。`KeyError` は列名のスペル違い、`SyntaxError` はカッコや全角・半角のことが多い。落ち着いて読めば、たいてい原因が書いてある。

---

## 1. データは「表」＝ DataFrame

CSVを読み込むと、Excelのような表になる。Pythonではこれを **DataFrame** と呼び、慣習的に `df` という名前を付ける。

```python
df = pd.read_csv(URL)     # 表を読み込む
df.shape                  # (行数=学生数, 列数)
list(df.columns)          # 列名の一覧
df.head()                 # 先頭5行
```

「行＝1人の学生」「列＝身長・テスト点などの項目」。これがデータ分析の基本の形だ。

---

## 2. 列を取り出す

1列を取り出すには `df["列名"]` と書く。

```python
df["テスト点"]      # テスト点の列
```

!!! warning "列名は正確に"
    `df["テスト店"]` のように1文字でも違うと `KeyError`。`list(df.columns)` で正しい綴りを確認するくせをつけよう。

---

## 3. 代表値をコードで（第2回の再現）

第2回で手計算した3つの代表値は、メソッド1つで出る。

```python
df["テスト点"].mean()    # 平均  → 57.4
df["テスト点"].median()  # 中央値 → 58
df["テスト点"].mode()    # 最頻値 → 53
```

第2回で電卓を叩いて出した値と、同じになるはず。**違いは速さと正確さだけ。**

---

## 4. ばらつきをコードで（第3回の再現）

```python
df["テスト点"].std()   # 標準偏差 → 11.6
df["テスト点"].var()   # 分散     → 133.8
```

第3回の「SD ≈ 11.6点」がそのまま出る。

---

## 5. describe() ―― 一気に要約

```python
df["テスト点"].describe()
```

件数・平均・SD・最小・四分位数・最大が一度に出る。手計算なら数十分の作業が一瞬で終わる。

---

## 6. グループごとに集計（groupby）

「学部ごとのテスト点平均」のような**グループ別集計**は、`groupby` で書く。

```python
df.groupby("学部")["テスト点"].mean()
# 経済学部 58.51 / 社会福祉学部 56.82 / 文学部 56.38
```

`groupby("グループにする列")["集計する列"].集計関数()` という形。これは第9回（分散分析）への布石でもある。

---

## 7. 欠損を確認

```python
df.isna().sum()   # 列ごとの欠損(未記入)の数
# 睡眠時間h: 2 / 通学時間min: 1
```

第2回で仕込んだ「汚れ」（未記入）が、コードだと一瞬で見つかる。現実のデータには必ず欠損がある。

---

## 8. グラフを1行で

```python
plt.hist(df["テスト点"], bins=25)
```

第2・3回で見たヒストグラムも、コードなら1行。日本語が□□に化けるときは、冒頭の `japanize-matplotlib` を実行したか確認する。

---

## まとめ

| やりたいこと | コード |
|---|---|
| 列を取り出す | `df["列名"]` |
| 平均・中央値・最頻値 | `.mean() .median() .mode()` |
| 標準偏差・分散 | `.std() .var()` |
| まとめて要約 | `.describe()` |
| グループ別集計 | `df.groupby("列")["列"].mean()` |
| 欠損の確認 | `df.isna().sum()` |
| ヒストグラム | `plt.hist(df["列"])` |

> 手で求めた値と、コードで求めた値は同じ。違うのは速さと正確さ。
> **計算は機械に任せ、人間は「どの数字を信じるか」の判断に集中する。**

!!! abstract "次回予告"
    第5回「相関と因果」。2つの列の関係を散布図と相関係数で見る。そして――「相関がある」ことと「因果がある」ことの、決定的な違いへ。
